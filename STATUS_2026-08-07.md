# 프로젝트 현황 정리 — 2026-08-07

---

# 0. 한 장 요약

| | |
|---|---|
| **목표** | 시뮬레이션이 없는 우주론 (Ωm, w0, wa)의 스냅샷을 보간으로 생성 |
| **현재 단계** | 1단계(전제 검증) **완료** → 2단계(실제 보간) 진입 직전 |
| **핵심 결론** | **전제가 성립한다.** 실제 격자 간격에서 86억 입자 중 41개만 기준 초과 |
| **이번 주** | 지난 미팅 지시 6건 중 4건 완료, 2건 부분 완료 |
| **다음 주** | **실데이터 보간** (본론) |

---

# 1. 프로젝트가 뭘 하려는 건가

## 문제

KIAS Multiverse는 우주론 파라미터 (Ωm, w0, wa)를 바꿔가며 **56개** N-body 시뮬레이션을
돌린 것이다. 하지만 파라미터 공간은 연속인데 시뮬은 56개 점뿐이다.

**"점과 점 사이의 우주론에서 스냅샷이 필요하면?"** — 새로 시뮬을 돌리면 슈퍼컴 수개월이
걸린다. 그래서 **기존 시뮬들로부터 보간해서 만들자**는 것이 이 프로젝트다.

## 왜 가능할 것 같은가 — 공유 초기조건

Multiverse의 56개는 **전부 같은 초기조건**에서 출발한다. 같은 위치에 같은 입자(같은 `indx`)를
놓고, **우주론 규칙만 바꿔서** z=47부터 z=0까지 굴린 것이다.

그러면 입자 #37752980은 56개 우주 모두에 존재하고, 태초 위치가 같다. 우주론이 다르면
z=0에 조금씩 다른 곳에 가 있을 뿐이다.

```
입자 37752980의 z=0 위치:
  Ωm=0.21에서 → (512.3, 200.1, 8.4)
  Ωm=0.26에서 → (512.4, 200.0, 8.5)
  Ωm=0.36에서 → (512.6, 199.8, 8.7)
                    ↑
        이 셋을 알면 Ωm=0.30에서의 위치를 사이값으로 추측 가능
```

**입자 단위로 짝지어 보간한다** — 이것이 이 프로젝트의 아이디어다.

## 그런데 전제가 있다

이게 성립하려면 **같은 입자가 우주론이 달라도 비슷한 위치에 있어야** 한다. 우주론 하나
바꿨다고 입자가 완전히 딴 데로 튀어버리면 짝을 지어도 의미가 없다.

Dr. Hong이 제시한 기준: **z=0까지 10 cMpc/h 이상 움직이지 않는가?**

지금까지의 작업은 **거의 전부 이 전제를 검증하는 것**이었다.

---

# 2. 프로젝트는 지금 어디에 있나

```
┌─────────────────────────────────────────────────────┐
│ 1단계: 전제 검증                            ✅ 완료 │
│   "같은 입자가 우주론 달라도 비슷한 위치에 있나?"    │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ 2단계: 실제 보간                        ⬜ 시작 전 │
│   "만들어서 진짜 맞는지 확인"                        │
└─────────────────────────────────────────────────────┘
```

**주의**: 보간 모델(linear/quadratic/RBF/GP) 코드는 있지만 **mock 데이터로만** 검증됐다.
실데이터에 적용한 적이 없다. 그게 2단계고, 다음 주 목표다.

---

# 3. 저번 미팅에서 무슨 일이 있었나

저번엔 **병렬화 17배만** 발표했다 (내용을 아직 소화 못 한 상태여서).

Dr. Hong의 반응은 **병렬화 방식 비판**이었다:

> "잡 내부에서 나누는 게 괜찮나? 다른 cpu나 다른 노드를 쓸 가능성이 없어지지 않아?
> 하나라도 죽으면 영향이 있을 거 같다. 이미 16개로 나눠진 그 하나를 독립 잡으로
> 섭밋하는 게 나아질 거 같다."

정확한 지적이었다. 그리고 과학 방향 5개를 추가로 주셨다:

| # | 지시 |
|---|---|
| ① | 16개를 **독립 job**으로 제출 |
| ② | **우주론을 더 많이** 써서 전체적으로 보자 |
| ③ | **어느 방향으로** 커지는지 |
| ④ | 각 포인트마다 **위치·속도**가 통계적으로 잘 나오는가 |
| ⑤ | 욕심내서 **2점 상관함수**도 맞으면 대박 |
| ⑥ | **거리가 어디까지 커지나? 커진다면 왜?** |

## ⚠️ 중요

**Ωm 사다리와 성장인자 결과는 아직 발표 안 됐다.** 지금까지 나온 가장 중요한 과학
결과인데, 저번엔 병렬화만 얘기해서 빠졌다. **이번 미팅이 그걸 보고할 첫 기회다.**

---

# 4. 이번 주에 한 것 — 지시사항 처리

## ① 독립 job — 완료, 그리고 실제로 효과가 있었다

**전에는**: SLURM job 하나를 받아서 그 안에서 파이썬 프로세스 16개를 `&`로 띄웠다.
```bash
for i in 0..15; do python3 verify_shared_ic.py ... & done
wait
```

**문제점** (지적하신 대로):
- 노드 하나에 갇힘 — 다른 노드를 못 씀
- 64코어를 잡고 16개만 사용 (48코어 낭비)
- 하나 죽으면 전체를 다시 돌려야 함

**지금은**: 각 워커가 **독립적인 SLURM job**이다.
```bash
sbatch --array=0-15 job_ic_array.sh ...          # 워커 16개
sbatch --dependency=afterok:<id> job_ic_merge.sh # 병합 (전부 성공해야)
```

**실제로 확인된 효과** — 오늘 돌린 job의 배치:
```
404759_0~11  → grammar019
404759_12    → grammar012
404759_13~15 → grammar011     ← 3개 노드에 분산!
```
이전 방식이었으면 64코어 노드가 통째로 빌 때까지 기다렸을 텐데, **6분 만에 전부 시작**했다.

**정확성 보장**: 통계량이 전부 덧셈(히스토그램, 카운터)이라 병합이 단일 실행과
**비트 단위로 동일**함을 검증했다. 그리고 오늘 실측으로도 확인됐다 —
```
독립 job (6870만 개 시점):  median 1.096
이전 풀스캔 (86억 개):       median 1.097
```

**왜 MPI가 아닌가**: 워커끼리 주고받을 데이터가 없다 (embarrassingly parallel).
MPI를 쓰면 의존성만 늘고 얻는 게 없다. 게다가 워커 하나가 죽어도 나머지는 살아남는다.

## ② 우주론 더 많이 — 진행 중

지금 7개 우주론의 z=0 P(k)를 동시에 계산 중이다. 그리고 |Δr| 스캔도 추가로 돌리고 있다.

**부수적으로 발견한 문제** (§7에서 자세히): SyncINITIAL의 step이 우주론마다 다르다.

## ③⑥ 방향과 이유 — 코드 완료, 실행 중

이 둘은 사실 같은 질문이라 **한 방법으로 동시에 답할 수 있다.**

### 아이디어

입자의 **자기 변위** ψ를 정의한다:
```
ψ = (현재 위치) − (태초 격자 위치)
```
태초 위치는 `indx`에서 복원할 수 있으므로 계산 가능하다.

이제 두 우주론 사이의 차이를 보면:
```
Δr = x_A − x_B = ψ_A − ψ_B
```

**만약** 우주론 차이가 단지 변위를 키우거나 줄이는 것이라면 (ψ_B = (1+ε)·ψ_A):
```
Δr = −ε · ψ_A        ← Δr이 ψ와 완전히 평행!
```

### 그래서 측정하는 것

```
cos(Δr, ψ)  = +1 또는 −1  →  순수한 성장 재스케일 (방향은 입자 자신의 이동 방향)
            =  0          →  무작위 (예측 불가)

|ψ|별 <|Δr|>              →  변위가 큰 곳에서 Δr도 큰가? (왜 커지는지)
```

**③(어느 방향)과 ⑥(왜 커지나)이 한 번에 답해진다.**

합성 데이터로 검증했다:

| 시나리오 | mean cos | collinear | \|ψ\|에 따른 증가 |
|---|---|---|---|
| 순수 성장 재스케일 | −1.000 | 100% | 252배 |
| 무작위 | −0.001 | 10% | 0.6배 |
| 혼합 | −0.636 | 31% | 1.6배 |

세 경우를 확실히 구분한다. **실데이터 결과는 지금 계산 중이다.**

## ④ 속도 — 부분 성공, 정직하게 보고할 것

### 문제: 변환 공식이 없다

원본 C 코드(`samples/`)를 뒤졌더니 `Fact1`, `Fact2`, `Pfact`가 **선언만 되어 있고**
실제로 쓰는 evolution 코드는 없었다. 게다가 실제 헤더를 읽어보니 **셋 다 값이 0**이다.

즉 **코드에서 km/s 변환을 얻을 수 없다.**

### 해결: 물리로 역산

선형이론에서 입자의 속도는 자기 변위에 묶여 있다:
```
v_peculiar = a · H(a) · f(a) · ψ
```
ψ를 이미 계산하니, `v_raw`를 ψ에 회귀시키면 변환계수가 나온다. `a`, `H`, `f`는
cosmoprimo로 구한다.

### 초기조건에서는 완벽하다

```
z = 47:
  cos(v, ψ) = +1.0000       ← 모든 |ψ| 구간에서
  v_raw/|ψ| 편차 0.07%      ← 완벽하게 평평한 plateau
  → 1 code unit = 3.25e5 km/s
```
검산도 맞는다: |v_raw| rms 3.26e-4 × 3.25e5 = **106 km/s**이고, 선형이론
`aHf·ψ = 317.5 × 0.334 = 106 km/s`와 일치한다.

부수적으로, `1/0.0009763 = 1024.28 ≈ boxsize`다. 즉 초기조건에서 저장된 속도는
**변위를 박스 단위로 표현한 값**과 수치적으로 같다.

### 하지만 다른 시점에서는 계수가 흔들린다

| z | cos(v, ψ) | 도출된 계수 (km/s) |
|---|---|---|
| 47.0 | **1.0000** | 3.25e5 |
| 1.0 | 0.9557 | 8.97e5 |
| 0.5 | 0.9307 | 1.06e6 |
| 0.0 | 0.8932 | **1.65e6** |

**cos이 11%만 어긋났는데 계수가 5.1배 변한다.** 비선형만으로는 설명이 안 된다.

### 원인 짐작

z=0에서 |ψ| rms는 11.4 cMpc/h인데, 내 코드는 plateau를 |ψ| = 0.03~0.1 구간에서 읽는다.
그 시점에 그렇게 안 움직인 입자는 "선형 영역의 입자"가 아니라 **변위가 0을 지나는 특이한
놈들**이다. 즉 엉뚱한 부분집합에서 재고 있을 가능성이 크다.

대안 가능성 — 저장된 변수가 정준운동량(a²ẋ 같은)이라 변환계수가 a에 의존한다 — 도
배제하지 못했다.

### 결론

⚠️ **km/s 값은 아직 쓰면 안 된다.** 제대로 하려면 저 z에서 **큰 스케일로 스무딩**해서
선형 모드만 보고 재야 한다.

단, **우주론 간 속도 차이는 code 단위로 비교 가능하다** (비율이라 단위가 무관하다).

## ⑤ 2점 상관함수 — 방법 확정, 재료 준비 중

### 먼저 비용을 계산했다

전에 "86억 개는 불가능"이라고 말했는데 **틀렸다.** 실제로 계산해보니:

```
메모리: float32 위치 = 103 GB   (노드 500 GB에 들어감)  ✅

시간 (r_max에 따라):
  r_max 10 Mpc/h  →   6 코어시간   ✅ 가능
  r_max 20        →  50 코어시간
  r_max 50        → 780 코어시간   ❌
  r_max 150 (BAO) → 21000 코어시간 ❌
```

폭발하는 이유는 N이 아니라 **밀도**다. (Mpc/h)³당 입자 8개라 이웃 수가 r³로 늘어난다.

### 그래서 P(k) → ξ(r) 변환을 선택

`cosmoprimo.fftlog.PowerToCorrelation` (이미 설치된 라이브러리). 몇 초면 된다.

**정확도를 실측했다** (주장이 아니라):
- 해석해(가우시안 P(k))와 비교: **상대오차 1e-5**
- 실제 측정 k범위(0.006~1.57, 120 bin)로 자르고 현실적 모양(P ~ k^-2.3)으로:
  **r=1~10에서 0.05%, r=10~100에서 0.3%**

부수적으로, damping 옵션이 **오히려 해롭다**는 것도 확인했다 (damping=1이면 오차 4%).
멱함수 외삽이 절단을 이미 잘 처리하기 때문이다. 문서의 조언을 정정했다.

**지금 P(k) 7개를 계산 중**이고, 나오면 ξ(r) 비율이 바로 나온다.

---

# 5. 아직 발표 안 한 과학 결과 — 이게 사실상 메인

## 결과 A — Ωm 축은 보간 가능하다

### 배경: 왜 이게 문제였나

전에 Ωm을 0.21↔0.36으로 바꿔봤더니 median |Δr|이 **2.51 cMpc/h**였다. 입자 간격(0.5)의
5배다. 게다가 44만 개가 10 cMpc/h를 넘었다. 위험 신호로 보였다.

### 그런데 그건 "끝에서 끝"이었다

실제 격자는 Ωm = 0.21, **0.26, 0.31**, 0.36이다. 보간은 **이웃 사이**(Δ0.05)에서 하지,
끝에서 끝(Δ0.15)으로 하지 않는다.

그래서 ΔΩm 사다리를 만들어 재봤다 (전부 z=0, w0=−1, wa=0, 86억 입자 풀스캔):

| ΔΩm | 쌍 | median | max | >10 cMpc/h |
|---|---|---|---|---|
| **0.05** ← 실제 격자 간격 | 0.21→0.26 | **1.097** | 12.65 | **41개** (4.8e-9) |
| 0.10 | 0.26→0.36 | 1.585 | 19.03 | 129,853 |
| 0.15 ← 끝에서 끝 | 0.21→0.36 | 2.512 | 19.87 | 443,621 |

**(1) 실제 쓰는 간격에서는 86억 개 중 41개만 기준을 넘는다.** 8자릿수 여유다.
지금까지의 걱정스러운 숫자는 전부 보간이 겪지 않는 경우였다.

### (2) 그리고 더 중요한 것 — 변위장이 계통적이다

0.15 단계는 앞 두 단계의 **합성**이다. 그래서 이걸 일관성 검정으로 쓸 수 있다:

| | median \|Δr\| |
|---|---|
| 0.21→0.26 | 1.097 |
| 0.26→0.36 | 1.585 |
| **제곱합** (무작위라면 이래야) | 1.927 |
| **단순합** (완전히 일관되면) | 2.681 |
| **실측 0.21→0.36** | **2.512** |

**단순합의 94%.** 입자들이 Ωm이 커질 때 **계속 같은 방향으로** 밀려간다. 흩어지는 게
아니다.

**이게 핵심이다**: 문제는 "많이 움직인다"가 아니라 "예측 불가능하게 움직인다"였을 텐데,
예측 불가능하지 않다. 매끄럽고 일관된 변위장은 정확히 보간이 잡아낼 수 있는 것이다.

### (3) 스케일링

|Δr| ∝ ΔΩm^0.75 — 완만한 sublinear, 포화 없음. 매끄럽고 단조로워서 피팅 가능하다.

## 결과 B — 이동은 성장인자로 결정된다 (단, 암흑에너지만)

6쌍의 풀스캔을 각 쌍의 **선형 성장 차이 ΔD/D** (z=47→0, cosmoprimo/CAMB)로 나눠봤다:

| 스캔 | 뭐가 다른가 | median | ΔD/D | median ÷ ΔD% |
|---|---|---|---|---|
| (−1.2,−0.8) vs (−1.0,−1.6), Ωm 0.26 | 암흑에너지 | 0.030 | **−0.28%** | **0.108** |
| w0: −1.0 vs −1.4, Ωm 0.21 | 암흑에너지 | 1.047 | 10.39% | **0.101** |
| Ωm 0.21 vs 0.36, w0=−1 | Ωm | 2.512 | 14.05% | 0.179 |
| Ωm 0.21 vs 0.36, w0=−1.4 | Ωm | 2.754 | 9.87% | 0.279 |

**암흑에너지 두 쌍이 7% 이내로 일치한다** — 이동거리가 **35배** 차이나는데도.
즉 이 방향에서는 |Δr| ∝ ΔD라는 단순 비례가 성립한다.

**Ωm 두 쌍은 그 선을 1.8~2.8배 벗어나고**, 자기들끼리도 안 맞는다 (ΔD가 14.1% vs 9.9%인데
median은 2.51 vs 2.75로 비슷). 즉 Ωm 방향에서는 이동이 **ΔD가 아니라 ΔΩm 자체**로
결정된다.

### 왜인가

- **암흑에너지**는 성장 **진폭**만 바꾼다 → 성장인자 정규화로 완전히 흡수 가능
- **Ωm**은 초기 파워스펙트럼의 **모양**(k_eq ∝ Ωm·h)과 σ8까지 바꾼다 → 장이 재조정되는 게
  아니라 **재구성**된다

**예측**: 성장인자 정규화는 w0/wa 방향을 완전히 잡고, Ωm 방향은 부분적으로만 잡을 것이다.
→ 이게 다음에 검증할 것.

## ⚠️ 정정 — 예전에 "w0/wa 축은 안전하다"고 한 것

그 근거였던 쌍이 **거의 축퇴**였다. Δw0=+0.2, Δwa=−0.8이 CPL에서 a≈0.75에 교차해서
두 우주론의 팽창사가 거의 같다 (**ΔD/D = 0.28%**). 그래서 median이 0.030으로 나온 것이지,
"암흑에너지는 무해해서"가 아니다.

**w0만 0.4 바꾸면 median이 35배 뛴다.** 축 이름이 아니라 **성장 차이**가 결정 요인이다.

---

# 6. 코드가 뭘 하는가

## 전체 흐름

```
데이터 읽기  →  전제 검증  →  통계/그림  →  (보간)  →  평가
  gotpm      verify_shared_ic   plot_*     mvinterp/  power_spectrum
                                                       correlation
```

## 6.1 데이터 읽기

### `gotpm.py` — 유일한 리더

GOTPM 바이너리를 numpy로 읽는다. **나머지 전부가 이걸 import한다.**

핵심은 `decode_positions()`다. 파일에는 **변위**만 저장돼 있고, 실제 위치는 계산해야 한다:
```python
ix = indx % mx                    # indx에서 태초 격자 좌표 복원
iy = (indx % mxmy) // mx
iz = indx // mxmy
pos = mod(저장된변위 + 격자좌표, nx) * (box/nx)     # 주기경계 처리 + 물리 단위
```

**원본 C 코드와 대조 확인했다** — `samples/read.c`의 `XofP` 매크로와 정확히 같다.

*왜 이렇게 만들었나*: 처음엔 스크립트마다 이 디코딩을 복붙해서 **4벌**이 돌아다녔다.
버그를 고치면 4곳을 고쳐야 해서 하나로 합쳤다.

## 6.2 전제 검증

### `verify_shared_ic.py` — 핵심 도구

두 우주론에서 같은 `indx`를 찾아 |Δr|을 잰다. **박스 전체 86억 입자.**

**왜 어려운가**: subfile이 z-슬랩이라, 같은 입자가 우주론마다 다른 subfile에 들어간다.
같은 subfile끼리 비교하면 **경계를 넘어간 입자가 빠지는데, 하필 그게 제일 많이 튄
놈들**이다. 첫 버전이 이 실수를 했다.

**해결** — `run_window()`: A의 슬랩 *j*를 B의 슬랩 *j±w*에서 찾는다. 입자가 슬랩
두께(4.3)보다 훨씬 적게 움직이므로 이웃만 뒤지면 충분하다. 주기경계도 처리한다.

측정하는 것:
- |Δr| 히스토그램, 백분위, 임계값 초과 개수 (구간별)
- **축별 rms** — 등방인가 특정 축 편향인가
- **슬랩별 median** — 첫/마지막이 이상하면 주기경계 오류
- **cos(Δr, ψ)** ← 이번 주 추가 (③⑥)
- top-20 movers 좌표, `--save-movers`로 임계값 초과분 전부

### `check_subfiles.py`

`run_window`가 "subfile j = z층 j"라는 가정 위에 서 있어서, 그걸 먼저 검증했다.
**결과: 250개 전부, 틈 0.000, 겹침 0.000.** 가정이 아니라 측정된 사실이 됐다.

### `calibrate_velocity.py` — 이번 주 추가

속도 변환계수를 선형이론으로 역산 (§4의 ④).

## 6.3 병렬 실행

### `submit_ic.sh` → `job_ic_array.sh` + `job_ic_merge.sh`

독립 job 16개 + 병합 job (afterok 의존). §4의 ①.

### `merge_stats.py`

워커들의 `.npz`를 더한다. 히스토그램·카운터는 덧셈, top-20은 합쳐서 다시 상위 20개.
**단일 실행과 비트 단위 동일**함을 검증했다.

### `submit_pk.sh` → `job_pk_array.sh` — 이번 주 추가

P(k)를 **우주론마다** 독립 job으로. P(k) 하나는 모든 입자가 한 메시에 올라가야 해서
워커로 못 쪼갠다 — 대신 우주론을 쪼갠다.

## 6.4 물리량 측정

| 파일 | 무엇 |
|---|---|
| `power_spectrum.py` | pypower로 P(k). `--stride`로 I/O 조절 |
| `correlation.py` | P(k) → ξ(r) FFTLog **(이번 주 추가)** |
| `snapshot_view.py` | z-슬랩 → 2D 밀도 지도 |
| `mvinterp/cpl_growth.py` | 성장인자 D(z), f(z) — cosmoprimo 래퍼 |

## 6.5 보간 모델 — 아직 mock만

| 파일 | 무엇 |
|---|---|
| `mvinterp/compare.py` | linear/quadratic/RBF/GP 4개 비교 (scipy) |
| `mvinterp/gpu_interp.py` | CuPy GPU 보간, 입자축 타일링 |
| `mvinterp/gp_interp.py` | GPyTorch GP + 불확실도 |
| `mvinterp/make_mock.py` | mock 스냅샷 생성 (실데이터 오면 삭제될 임시) |

mock 검증 결과 (48³ × 36 우주론, median 위치오차 cMpc/h):

| | linear | quadratic | RBF | GP |
|---|---|---|---|---|
| 격자 밖 | 0.59 | 0.23 | **0.072** | 0.27 |
| leave-one-out | 0.031 | 0.080 | **0.0008** | 0.014 |

RBF가 linear보다 8~40배 정확하다. GP는 불확실도도 준다.

---

# 7. 정정 사항

## 7.1 SyncINITIAL step이 우주론마다 다르다

내가 문서에 **"11개 step이 모든 우주론에 동일"**이라고 적었는데 **틀렸다**:

```
Om0.26_-1.2_-0.8:  00001 00281 00345 00441 00601 00921 01090 01241 01561 01706 01881  (11개)
Om0.21_-1_0:       00001       00345       00601 00921 01090 01241 01561       01631 01881  (9개)
                         ↑없음       ↑없음                                ↑없음  ↑추가
```

**공통은 8개**: `00001 00345 00601 00921 01090 01241 01561 01881`

두 우주론(둘 다 Ωm=0.26)만 보고 일반화한 것이 원인이다.

**함의**: 보간 격자를 정하기 전에 **전체 우주론에서 step을 세어야 한다.**

## 7.2 "86억 개는 2PCF 불가능"이 틀렸다

메모리는 103 GB로 노드에 들어가고, 시간은 r_max에 달렸다 (r_max 10이면 6 코어시간).
근거 없이 말했던 것을 정정했다. §4의 ⑤ 참고.

## 7.3 "w0/wa 축은 안전하다"가 틀렸다

축퇴 쌍 하나를 보고 내린 결론이었다. §5의 정정 참고.

---

# 8. 미팅에서 할 말

## 순서

### 1. 아직 보고 안 한 과학 결과 (§5)
- **Ωm 축은 보간 가능하다** — 실제 격자 간격에서 41/86억, 변위장이 계통적(94%)
- **이동은 성장인자로 결정된다** — 단, 암흑에너지만. Ωm은 P(k) 모양까지 바꿔서 벗어남
- 정정: "w0/wa 안전"은 축퇴 쌍 때문에 생긴 착시였음

### 2. 지시사항 처리 (§4)
- ① **독립 job 완료** — 실제로 3개 노드에 분산됐고, 결과가 이전과 일치함을 확인
- ③⑥ **방향 측정 방법** — ψ와의 cos으로 두 질문을 동시에 답하는 설계. (결과 나왔으면 숫자)
- ④ **속도** — 초기조건은 완벽, 저 z는 미해결. 원인 짐작과 해결 방향
- ⑤ **2PCF** — 비용 계산 → FFTLog 선택, 정확도 실측 (1e-5 / 0.05~0.3%)

### 3. 정정 사항 (§7)
- SyncINITIAL step이 우주론마다 다름 → 격자 정하기 전에 전수 조사 필요

### 4. 다음 주
> **"실데이터 보간을 다음 주에 가져오겠습니다."**

전제 검증이 끝났고 결과가 좋으니, 이제 본론으로 넘어간다. 구체적으로:
- Ωm = 0.26을 빼고 0.21, 0.36으로 예측
- 실제 0.26과 입자별 비교
- P(k)와 ξ(r) 비율로도 평가

## 물어볼 것

1. **본론(보간)으로 가도 되나?** 아니면 전제 검증을 더 할까?
2. **성장인자 정규화 후 재측정을 먼저 할까?** (예측 검증)
3. **보간 격자를 어떻게 정할까?** — step이 우주론마다 다른 문제

---

# 9. 정직하게 말할 것

- **암흑에너지 비례 법칙은 점 2개**에 의존한다. 시사적이지만 "법칙"이라 부르기엔 이르다.
- **ΔΩm=0.10 사다리만 시작점이 0.26**이다 (Ωm=0.31에 z=0 스냅샷이 없어서). 완벽히 통제된
  사다리는 아니다. 다만 일관성 검정은 영향받지 않는다 (0.21→0.36이 합성이라는 사실만 씀).
- **일관성 94%를 median으로** 계산했다. 엄밀히는 변위 **벡터의 내적**으로 봐야 한다.
- **속도는 초기조건에서만** 검증됐다. km/s 값을 아직 쓸 수 없다.
- **보간 정확도는 전부 mock 수치**다. 실데이터 값은 존재하지 않는다.
- ③⑥⑤는 **코드는 됐지만 결과는 계산 중**이다.

---

# 10. 그림

| 파일 | 무엇 | 중요도 |
|---|---|---|
| `om_ladder.png` | ΔΩm 사다리 → Ωm 보간 가능 | ★★ |
| `dr_scaling.png` | 이동 ∝ 성장인자 (암흑에너지만) | ★★ |
| `movers_proj.png` | movers 위치, smoke vs 풀스캔 | ★ |
| `hist_om21v26.png` | \|Δr\| 전체 히스토그램 + 슬랩 + 축 진단 | ★ |
| `w0wa_grid.png` | (w0,wa) 격자 | |
| `methods_results.png` | 보간 모델 4개 (mock) | |
